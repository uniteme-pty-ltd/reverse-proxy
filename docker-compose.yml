version: '3.9'

name: reverse-proxy

services:

  #################
  # REVERSE PROXY #
  #################
  
  api:
    container_name: reverse-proxy
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PROFILE: dev # Faster build times for local develpment, this will be automatically changed to release if omitted
    ports:
      - "80:80"
      - "443:443"
    environment:
      USE_SELF_SIGNED_CERT: false
      USE_LETSENCRYPT_PROD: false
      DOMAIN_MAPS: localhost.com|example.com:80